<!-- All launch files needed in ordre to do some basic motion palnning  -->
<launch>

    <!-- launch robot sim -->
    <include file="$(find wheelchair)/launch/wheelchair_gazebo.launch"/>

    <!-- launch moveit using the configuration generated by the wizard, for path planning and accepting goals -->
    <include file="$(find wheelchair_moveit_config)/launch/move_group.launch"/>
    
    <!-- <node pkg="moveit_ros_move_group" type="move_group" name="remap_to_REACH" output="screen">
    <remap from="joint_states" to="REACH/joint_states"/>
    </node> -->


    <!-- use rviz to send configuration to send goals -->
    <include file="$(find wheelchair_moveit_config)/launch/moveit_rviz.launch">
        <arg name="config" value="true"/>
    </include>
    
</launch>