# Tell MoveIt! where to find follow_joint_trajectory action server that's generated by ros_control plugin, & also which joints are to be controllerd.
controller_manager_ns: /
controller_list:
  - name: arm_controller
    action_ns: follow_joint_trajectory
    type: FollowJointTrajectory
    joints:
      - joint0
      - joint1